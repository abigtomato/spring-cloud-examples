server:
  port: 82

spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host: 192.168.121.100
      port: 8500
      discovery:
        service-name: ${spring.application.name}